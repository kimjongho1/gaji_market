<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC " -//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="norification">

	<select id="count" parameterType="string" resultType="_int">		<!-- 안읽은 알림갯수 획득 -->
		select count(*) from notification where user_id=#{userId} and read_yn='N'
	</select>
	
	<update id="updateNotiYN" parameterType="_int"> <!-- 알림 클릭시 알림 읽음상태로 전환 -->
		update notification set read_yn='Y' where noti_id=#{notiId}
	</update>
	
	<select id="countNotification">	<!-- 알림갯수 -->
		select count(*) from notification where user_id=#{} and read_yn='N'
	</select>
	
	<select id="getCommentNotice"> <!-- 해당계정의 댓글알림 -->
		select noti_id,message,read_yn,type,reference_id,created_at from notification where type=1 and user_id=#{user_id}
	</select>
	
	<select id="getKeywordNotice"> <!-- 해당계정의 키워드알림 -->
		select noti_id,message,read_yn,type,reference_id,created_at from notification where type=2 and user_id=#{user_id}
	</select>
	
	<select id="getTradeNotice"> <!-- 해당계정의 거래알림 -->
		select noti_id,message,read_yn,type,reference_id,created_at from notification where type=3 and user_id=#{user_id}
	</select>
	
</mapper>
